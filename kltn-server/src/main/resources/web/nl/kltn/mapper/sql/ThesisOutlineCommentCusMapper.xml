<!DOCTYPE mapper
  PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
  "https://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="web.nl.kltn.mapper.ThesisOutlineCommentCusMapper">

	<select id="searchByThesisId" resultType="web.nl.kltn.model.generator.ThesisOutlineComment">
		SELECT
			id,
			thesis_id,
			user_id,
			comment,
			order,
			is_deleted,
			created_at,
			update_at
		FROM
			thesis_outline_comment
		WHERE
			thesis_id = #{thesisId}
			AND is_deleted = 0
		ORDER BY order
	</select>
	
	<select id="searchByUserId" resultType="web.nl.kltn.model.generator.ThesisOutlineComment">
		SELECT
			id,
			thesis_id,
			user_id,
			comment,
			order,
			is_deleted,
			created_at,
			update_at
		FROM
			thesis_outline_comment
		WHERE
			user_id = #{user_id}
			AND is_deleted = 0
		ORDER BY order
	</select>

</mapper>