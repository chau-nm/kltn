<!DOCTYPE mapper
  PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
  "https://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="web.nl.kltn.mapper.ThesisLeturerCusMapper">

    <resultMap type="web.nl.kltn.model.generator.ThesisLeturer" id="ThesisLeturer">
    	<id column="id" property="id" jdbcType="VARCHAR"/>
    	<result column="thesis_id" property="thesisId" jdbcType="VARCHAR"/>
    	<result column="leturer_id" property="leturerId" jdbcType="VARCHAR"/>
    	<result column="is_active" property="isActive" jdbcType="BIT"/>
    	<result column="is_deleted" property="isDeleted" jdbcType="BIT"/>
    	<result column="created_at" property="createdAt" jdbcType="BIGINT"/>
    	<result column="updated_at" property="updatedAt" jdbcType="BIGINT"/>
    </resultMap>

	<select id="getThesisLeturerByThesisId" resultMap="ThesisLeturer">
		SELECT
			id,
			thesis_id,
			leturer_id,
			is_active,
			is_deleted,
			created_at,
			updated_at
		FROM
			thesis_leturer
		WHERE
			thesis_id = #{thesisId}
	</select>

</mapper>