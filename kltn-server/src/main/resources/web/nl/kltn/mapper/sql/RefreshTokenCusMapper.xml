<!DOCTYPE mapper
  PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
  "https://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="web.nl.kltn.mapper.RefreshTokenCusMapper">
	<select id="findAvailabilityByUserId" parameterType="java.lang.String" resultType="web.nl.kltn.model.generator.RefreshToken">
		SELECT
			token,
			user_id,
			expiration_date,
			create_at,
			update_at
		FROM
			refresh_token
		WHERE 
			user_id = '${userId}'
		AND
			expiration_date >= now()
	</select>
</mapper>